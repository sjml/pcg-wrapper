language: node_js
node_js:
  - 'node'
matrix:
  include:
    - os: 'osx'
  include:
    - os: 'linux'
      env: CXX=g++-4.8
      addons:
        apt:
            sources:
              - ubuntu-toolchain-r-test
            packages:
              - g++-4.8
